events {}

http {
  server {
    listen 80;

    location /api {
      proxy_pass http://localhost:4000;
    }

    location / {
      return 200 'Hello from Nginx!';
      add_header Content-Type text/plain;
    }
  }
}
